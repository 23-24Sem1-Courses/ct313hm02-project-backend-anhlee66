<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <script>
        async function login(user) {
            let url = `${baseURL}/login`
            const options = {
                method: 'POST',
                headers,
                body: JSON.stringify(user)
            }
            const res = await fetch(url, options)
            // console.git log(res)
            if (res.status == 301) {
                console.log('Cannot Log in');
   //             store.commit('loginFailure')
            }
            else {
   //             store.commit('loginSuccess', user.email)
    //            console.log(localStorage.getItem('user'))
    //            $router.push({ name: 'homepage' })
    console.log('login')
            }
            }
    </script>
    
</head>
<body>
    <div class="root">
        <h2 class="text-center">Welcome to Doc Sharing</h2>
        <div class="main">
            <form class="form-input" onsubmit="login()" method="post">
                <div class="form-group">
                    <label for="email">Email address</label>
                    <input name="email" type="email" class="form-control" />
                    {{!--
                    <ErrorMessage name="email" class="error-feedback text-danger" /> --}}
                </div>
                <div class="form-group">
                    <label for="password">Password</label>
                    <input name="password" type="password" class="form-control" s />
                    {{!--
                    <ErrorMessage name="password" class="error-feedback text-danger" /> --}}
                </div>
                <div class="form-group ">
                    <button class="btn btn-primary  justify-content-center">Login</button>
                </div>
                <a href="/signup">Sign up if you don't have Account</a>
        
            </form>
        </div>
    </div>
</body>
</html>